<project name="slim3-mvc" default="dist" basedir=".">

	<import file="../common.xml"/>
	
	<target name="dist" depends="clean,compile,copy">
		<jar jarfile="../target/slim3-mvc-${ver}.jar" basedir="${build}" />
        <jar jarfile="../target/slim3-mvc-${ver}-sources.jar" basedir="${src}" />
        <delete dir="target"/>
	</target>

</project>